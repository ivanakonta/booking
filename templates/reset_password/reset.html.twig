{% extends 'reset_password/base-reset.html.twig' %}

{% set pageTitle = 'Reset your password' %}

{% block content %}
         <div class="authincation h-100">
        <div class="container h-100">
            <div class="row justify-content-center h-100 align-items-center">
                <div class="col-md-6">
                    <div class="authincation-content">
                        <div class="row no-gutters">
                            <div class="col-xl-12">
                                <div class="auth-form">
                                    <div class="text-center mb-3">
                                        <img src="{{ asset('images/pilane1.png')}}" alt="" width="350" height="85">
                                    </div>
                                    <h4 class="text-center mb-4">Restartirajte lozinku</h4>

                                    {{ form_start(resetForm) }}
                                    <div class="form-group">
                                        <div class="form-group">
                                            <label class="mb-1"><strong>Nova lozinka</strong></label>
                                            {{ form_row(resetForm.plainPassword.first,{'attr': {'class': 'form-control'}, 'label': false}) }}
                                        </div>
                                        <div class="form-group">
                                            <label class="mb-1"><strong>Ponovno upiši novu lozinku</strong></label>
                                            {{ form_row(resetForm.plainPassword.second,{'attr': {'class': 'form-control'}, 'label': false}) }}
                                        </div>
                                    </div>
                                    <div class="text-center">
                                        <button class="btn btn-primary btn-block">Restartiraj lozinku</button>
                                    </div>
                                    {{ form_end(resetForm) }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {# {{ form_start(resetForm) }} #}
        {# {{ form_row(resetForm.plainPassword) }} #}
        {# <button class="btn btn-primary">Reset password</button> #}
    {# {{ form_end(resetForm) }} #}
{% endblock %}
